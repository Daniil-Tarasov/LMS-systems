events {
    worker_connections 1024;
}

http {
    upstream django {
        server lms_systems:8000;
    }

    server {
        listen 80;
        server_name _;

        location / {
            proxy_pass http://django;
        }
    }
}