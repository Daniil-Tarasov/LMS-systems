[Unit]
Description=LMS Systems Docker Container
Requires=docker.service
After=docker.service

[Service]
Restart=always
EnvironmentFile=/LMS-systems/.env
ExecStart=/usr/bin/docker run --name lms_systems -p 80:8000 --env-file /LMS-systems/.env ontarasoff/lms_systems:latest
ExecStop=/usr/bin/docker stop lms_systems

[Install]
WantedBy=multi-user.target